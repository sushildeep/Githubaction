name: shell Commands
on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-18.04
    steps:
      - name: echo a string
        run: echo "Global logic"
      - name: multiline script
        run: |
          node -v
          npm -v  